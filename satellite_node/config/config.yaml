satellite_node:
  ros__parameters:
    tile_zoom: 18          # 400m 고도 등가 (1px ≈ 0.5m)
    grid_tiles: 7          # 7×7 타일 → 약 900m × 900m 커버리지
    publish_rate_hz: 1.0
    output_size: 1280
    tile_cache_dir: "/home/kimseongmin/.cache/bootcamp_tiles"
